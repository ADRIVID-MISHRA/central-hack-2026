import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const firstTimeUser = (props: Props) => {
  return (
    <View>
      <Text>first-time-user</Text>
    </View>
  )
}

export default firstTimeUser

const styles = StyleSheet.create({})